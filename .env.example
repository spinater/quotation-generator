# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Your PostgreSQL connection details (Zed MCP format)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=quotation_db
DB_USERNAME=postgres
DB_PASSWORD=your_password_here

# Prisma requires DATABASE_URL - constructed from the variables above
# Replace the values below with your actual credentials
DATABASE_URL="postgresql://${DB_USERNAME}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?schema=public"

# OR if your shell doesn't support variable substitution in .env, use the literal format:
# DATABASE_URL="postgresql://postgres:your_password_here@localhost:5432/quotation_db?schema=public"

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Next.js public URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Node environment
NODE_ENV="development"

# =============================================================================
# INSTRUCTIONS FOR YOUR SETUP
# =============================================================================
#
# 1. Copy this file to .env in the project root:
#    cp .env.example .env
#
# 2. Update the values with your actual PostgreSQL credentials:
#    - DB_HOST: Your PostgreSQL host (likely 'localhost')
#    - DB_PORT: Your PostgreSQL port (default is 5432)
#    - DB_NAME: Your database name (e.g., 'quotation_db')
#    - DB_USERNAME: Your PostgreSQL username
#    - DB_PASSWORD: Your PostgreSQL password
#
# 3. The DATABASE_URL will be constructed automatically if your shell supports
#    variable substitution. If not, manually construct it using this format:
#    DATABASE_URL="postgresql://USERNAME:PASSWORD@HOST:PORT/DBNAME?schema=public"
#
#    Example:
#    DATABASE_URL="postgresql://postgres:mypassword@localhost:5432/quotation_db?schema=public"
#
# 4. After creating .env, test your connection:
#    npm run test:db
#
# =============================================================================
# SECURITY NOTES
# =============================================================================
#
# - NEVER commit the .env file to version control
# - The .env file is already in .gitignore
# - Keep your database credentials secure
# - Use strong passwords for production environments
#
# =============================================================================
